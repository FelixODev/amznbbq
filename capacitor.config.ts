import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.amznbbq',
  appName: 'amznbbq',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
